import {palette} from '../palette';

export const dark = {
    dark: true,
    colors: {
        primary: palette.blue,
        background: palette.white,
        text: palette.blue,
        border: palette.blue,
    },
};
